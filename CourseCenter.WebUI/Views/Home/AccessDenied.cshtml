
@{
    ViewData["Title"] = "Error Sayfasi";
    Layout = "~/Views/Shared/_ErrorLayout.cshtml";
}

@* <div class="center">
    <br />
    <h1>ACCESSDENIED ! -- Erişim Engellendi.</h1>
    <br />
    <h1>BU SAYFAYA ERISMEK ICIN YÖNETİCİ OLARAK GİRİŞ YAPIN !</h1>
    <br />
    @if (ViewBag.ReturnUrl != null)
    {
        <a class="btn btn-primary" asp-action="login" asp-controller="Auth" asp-route-ReturnUrl="@ViewBag.ReturnUrl">Giriş Sayfasına</a>
    }
    else
    {
        <a class="btn btn-primary" asp-action="login" asp-controller="Auth">Giriş Sayfasına</a>
    }
</div>

<!DOCTYPE html>
<html dir="ltr"> *@


        <div class="error-box">
            <div class="error-body text-center">
                <h1 class="error-title text-danger">403</h1>
                <h3 class="text-uppercase error-subtitle">FORBIDDEN !</h3>
                <p class="text-muted m-t-30 m-b-30">YOU SEEM TO BE TRYING TO FIND HIS WAY HOME OR LOGIN</p>
                @* <a href="index.html" class="btn btn-danger btn-rounded waves-effect waves-light m-b-40">Back to home</a> *@
                <a class="btn btn-primary btn-rounded waves-effect waves-light m-b-40" asp-action="index" asp-controller="Home">Ana Sayfaya</a>
        <a class="btn btn-danger btn-rounded waves-effect waves-light m-b-40" asp-action="login" asp-controller="Auth" asp-route-ReturnUrl="@ViewBag.ReturnUrl">Giriş Sayfasına</a>
            </div>
        </div>
